<UserControl x:Class="Organizer.UserControls.Views.DailyChoreView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Organizer.UserControls.Views"
             xmlns:ucvms="clr-namespace:Organizer.UserControls.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Button Grid.Column="0"
                Click="CheckDailyChoreButton_Click"
                x:Name="CheckDailyChoreButton"
                Content="Check"/>

        <Label Grid.Column="1"
               Foreground="White"
               Content="{Binding AssignedChore.Description,
            RelativeSource={RelativeSource AncestorType=local:DailyChoreView}}"/>

        <Label Grid.Column="2"
               Foreground="White"
               Content="{Binding IsAccomplishedString,
            RelativeSource={RelativeSource AncestorType=local:DailyChoreView}}"/>

    </Grid>
</UserControl>
